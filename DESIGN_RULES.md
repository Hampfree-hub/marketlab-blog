# 📐 Правила оформления сайта MarketLab Academy

**Версия:** 2.1  
**Дата создания:** 20.12.2025  
**Последнее обновление:** 06.01.2026

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Этот документ содержит правила оформления для всего сайта. При добавлении новых элементов или изменении существующих — ОБЯЗАТЕЛЬНО сверяйся с этим документом!**

---

## 1. Толщина линий (Border Width)

### 🎯 Единое правило

**Абсолютно все линии, независимо от их назначения, должны иметь единую толщину.**

### 📏 Стандарт

```css
--nes-border-width: 1px; /* Вмеру жирная ширина для всех линий */
```

### ✅ Где применяется

- **Рамки контейнеров** (`border: var(--nes-border-width) solid var(--nes-black)`)
- **Рамки кнопок** (`border: var(--nes-border-width) solid var(--nes-black)`)
- **Разделители** (`border-bottom: var(--nes-border-width) solid var(--nes-gray)`)
- **Outline для focus** (`outline: var(--nes-border-width) solid var(--nes-yellow)`)
- **Все границы элементов** (карточки, панели, баннеры, формы)

### ❌ Запрещено

- Использовать `1px`, `2px`, `4px` или другие значения вместо `var(--nes-border-width)`
- Делать исключения для "тонких" или "толстых" элементов
- Использовать разные толщины для разных типов элементов

### 📝 Примеры правильного использования

```css
/* ✅ ПРАВИЛЬНО */
.article-card {
    border: var(--nes-border-width) solid var(--nes-black);
}

.button {
    border: var(--nes-border-width) solid var(--nes-black);
}

.separator {
    border-bottom: var(--nes-border-width) solid var(--nes-gray);
}

/* ❌ НЕПРАВИЛЬНО */
.article-card {
    border: 2px solid var(--nes-black); /* Разная толщина! */
}

.button {
    border: 1px solid var(--nes-black); /* Слишком тонко! */
}
```

---

## 2. Скругление углов (Border Radius)

### 🎯 Единое правило

**Все элементы должны иметь одинаковое скругление углов для единообразия.**

### 📏 Стандарт

```css
border-radius: 4px; /* Единое скругление для всех элементов */
```

### ✅ Где применяется

- Все кнопки
- Все контейнеры и карточки
- Все панели и блоки
- Все формы и инпуты
- Все изображения (если нужно)

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО */
.button {
    border-radius: 4px;
}

.card {
    border-radius: 4px;
}

/* ❌ НЕПРАВИЛЬНО */
.button {
    border-radius: 2px; /* Другое скругление! */
}

.card {
    border-radius: 8px; /* Другое скругление! */
}
```

---

## 3. Тени (Box Shadow)

### 🎯 Единое правило

**Все тени ЗАПРЕЩЕНЫ. Используем только плоские элементы без теней.**

### 📏 Стандарт

```css
/* БЕЗ теней - плоские элементы */
/* НЕ используем box-shadow */
```

### ✅ Где применяется

- Нигде! Все элементы плоские, без теней.

### ❌ Запрещено

- Использовать `box-shadow` в любом виде
- Горизонтальные тени (`Xpx 0px`)
- Диагональные тени (`Xpx Ypx`)
- Вертикальные тени (`0 Ypx`)
- Inset тени (`inset ...`)

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО */
.card:hover {
    transform: translateY(-1px);
    /* БЕЗ теней - плоские элементы */
}

/* ❌ НЕПРАВИЛЬНО */
.card:hover {
    box-shadow: 0 3px 0px rgba(0, 0, 0, 0.3); /* Тени запрещены! */
}
```

---

## 4. Beveling (Скошенные края) — создание 3D объёма

### 🎯 Единое правило

**Все кнопки и интерактивные элементы должны использовать beveling для создания 3D объёма в 8-битном стиле.**

### 📏 Стандарт

**Beveling (скошенные края) — главный трюк для создания объёма:**

- **Верхний-левый край** → светлый цвет (`--nes-bevel-light: #FFFFFF`) — источник света
- **Нижний-правый край** → тёмный цвет (`--nes-bevel-dark: #000000`) — тень

Это создаёт впечатление выпуклости или выступа (выпуклость кнопки).

### ✅ Где применяется

- Все кнопки (`.nes8bit-btn`, `.theme-toggle-btn`, `.language-toggle-btn`, `.cta-button`, `.cookie-banner-accept`, `.subscribe-button` и т.д.)
- Все карточки (`.article-card`)
- Все интерактивные элементы (`.nes8bit-nav a`, `.footer-social-link`, `.nes8bit-sidebar li`)

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО - базовое состояние (выпуклая кнопка) */
.button {
    border-top: var(--nes-border-width, 2px) solid var(--nes-bevel-light, #FFFFFF);
    border-left: var(--nes-border-width, 2px) solid var(--nes-bevel-light, #FFFFFF);
    border-bottom: var(--nes-border-width, 2px) solid var(--nes-bevel-dark, #000000);
    border-right: var(--nes-border-width, 2px) solid var(--nes-bevel-dark, #000000);
}

/* ✅ ПРАВИЛЬНО - hover (beveling сохраняется) */
.button:hover {
    background: var(--nes-yellow);
    transform: translateY(-1px);
    /* Beveling сохраняется для объёма */
}

/* ✅ ПРАВИЛЬНО - active (инвертируем beveling - вдавленная кнопка) */
.button:active {
    transform: translateY(1px);
    /* Инвертируем beveling при нажатии */
    border-top: var(--nes-border-width, 2px) solid var(--nes-bevel-dark, #000000);
    border-left: var(--nes-border-width, 2px) solid var(--nes-bevel-dark, #000000);
    border-bottom: var(--nes-border-width, 2px) solid var(--nes-bevel-light, #FFFFFF);
    border-right: var(--nes-border-width, 2px) solid var(--nes-bevel-light, #FFFFFF);
    transition: transform 0.05s ease, border 0.05s ease;
}

/* ❌ НЕПРАВИЛЬНО - без beveling */
.button {
    border: var(--nes-border-width, 2px) solid var(--nes-black); /* Нет объёма! */
}
```

### 🎨 CSS-переменные для beveling

```css
:root {
    --nes-bevel-light: #FFFFFF; /* Светлый край (верхний-левый) */
    --nes-bevel-dark: #000000;  /* Тёмный край (нижний-правый) */
}
```

---

## 5. Унифицированные кнопки (Buttons)

### 🎯 Единое правило

**Все кнопки должны использовать единую механику hover/active, как у кнопок темы и языка.**

### 📏 Стандарт

**Базовое состояние:**
- Плоские кнопки (без теней)
- Beveling для объёма (светлый верхний-левый, тёмный нижний-правый)

**Hover состояние:**
- `background: var(--nes-yellow)` — жёлтый цвет
- `transform: translateY(-1px)` — чуть выезжает вверх
- Beveling сохраняется

**Active состояние:**
- `transform: translateY(1px)` — кнопка прожимается вниз
- Beveling инвертируется (тёмный верхний-левый, светлый нижний-правый)
- `transition: transform 0.05s ease, border 0.05s ease`

### ✅ Где применяется

- Все кнопки на сайте
- Все карточки при клике
- Все интерактивные элементы

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО - полная реализация */
.button {
    background: var(--nes-white);
    border-top: var(--nes-border-width, 2px) solid var(--nes-bevel-light);
    border-left: var(--nes-border-width, 2px) solid var(--nes-bevel-light);
    border-bottom: var(--nes-border-width, 2px) solid var(--nes-bevel-dark);
    border-right: var(--nes-border-width, 2px) solid var(--nes-bevel-dark);
    transition: all 0.15s ease;
}

.button:hover {
    background: var(--nes-yellow);
    transform: translateY(-1px);
    /* Beveling сохраняется */
}

.button:active {
    transform: translateY(1px);
    border-top: var(--nes-border-width, 2px) solid var(--nes-bevel-dark);
    border-left: var(--nes-border-width, 2px) solid var(--nes-bevel-dark);
    border-bottom: var(--nes-border-width, 2px) solid var(--nes-bevel-light);
    border-right: var(--nes-border-width, 2px) solid var(--nes-bevel-light);
    transition: transform 0.05s ease, border 0.05s ease;
}
```

---

## 6. Анимации (Transform)

### 🎯 Единое правило

**Все hover-анимации должны быть только вертикальными (движение вверх).**

### 📏 Стандарт

```css
/* Небольшое выдвижение вверх (как у кнопок темы и языка) */
transform: translateY(-1px);
```

### ✅ Где применяется

- Все кнопки при hover
- Все карточки при hover
- Все интерактивные элементы

### ❌ Запрещено

- Горизонтальные движения (`translateX`)
- Диагональные движения (`translate(-Xpx, -Ypx)`)
- Только `translateY(-1px)` формат

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО */
.button:hover {
    transform: translateY(-1px);
}

/* ❌ НЕПРАВИЛЬНО */
.button:hover {
    transform: translate(-2px, -2px); /* Диагональное движение! */
}
```

---

## 7. Пространство и отступы (Spacing) — канон 8-битного стиля

### 🎯 Единое правило

**Все отступы, padding, margin и gap должны быть кратны 4px. Никогда нечётные пиксели!**

### 📏 Стандарт

**Базовые значения спейсинга (все кратны 4px):**

```css
--nes-spacing-xs: 4px;   /* Минимальный отступ (между иконкой и текстом) */
--nes-spacing-sm: 8px;   /* Малый отступ (между кнопками в группе) */
--nes-spacing-md: 16px;  /* Средний отступ (padding внутри карточки) */
--nes-spacing-lg: 24px;  /* Большой отступ (между секциями) */
--nes-spacing-xl: 32px;  /* Очень большой отступ (между крупными блоками) */
```

### ✅ Где применяется

- **Gap между элементами** (`gap: var(--nes-spacing-sm)`)
- **Padding внутри элементов** (`padding: var(--nes-spacing-md)`)
- **Margin между блоками** (`margin-bottom: var(--nes-spacing-lg)`)
- **Отступы в layout** (header, sidebar, content)

### ❌ Запрещено

- Нечётные пиксели (`5px`, `7px`, `13px`, `17px`, `19px`, `21px`, `23px`, `25px`, `27px`, `29px`, `31px`)
- Произвольные значения (`10px`, `15px`, `22px`)
- Только значения, кратные 4px!

### 📝 Таблица базовых значений

| Значение | Использование | Пример |
|----------|---------------|--------|
| `4px` | Минимальный отступ | Между иконкой и текстом в кнопке |
| `8px` | Малый отступ | Gap между кнопками в группе |
| `16px` | Средний отступ | Padding внутри карточки |
| `24px` | Большой отступ | Margin между секциями |
| `32px` | Очень большой отступ | Margin между крупными блоками |

### 📐 Демонстрация разных gap'ов

```css
/* ✅ ПРАВИЛЬНО - gap кратен 4px */
.button-group {
    display: flex;
    gap: var(--nes-spacing-sm); /* 8px */
}

.card-grid {
    display: grid;
    gap: var(--nes-spacing-md); /* 16px */
}

/* ❌ НЕПРАВИЛЬНО - нечётные пиксели */
.button-group {
    gap: 7px; /* Запрещено! */
}

.card-grid {
    gap: 13px; /* Запрещено! */
}
```

### 📦 Правила для padding внутри элементов

```css
/* ✅ ПРАВИЛЬНО - padding кратен 4px */
.button {
    padding: var(--nes-spacing-sm) var(--nes-spacing-md); /* 8px 16px */
}

.card {
    padding: var(--nes-spacing-md); /* 16px */
}

.section {
    padding: var(--nes-spacing-lg); /* 24px */
}

/* ❌ НЕПРАВИЛЬНО - нечётные пиксели */
.button {
    padding: 5px 10px; /* Запрещено! */
}

.card {
    padding: 15px; /* Запрещено! */
}
```

### 🏗️ Классический layout с header-sidebar-content

```css
/* ✅ ПРАВИЛЬНО - все отступы кратны 4px */
.layout {
    display: grid;
    grid-template-columns: 250px 1fr;
    gap: var(--nes-spacing-md); /* 16px */
}

.header {
    padding: var(--nes-spacing-md); /* 16px */
    margin-bottom: var(--nes-spacing-md); /* 16px */
}

.sidebar {
    padding: var(--nes-spacing-md); /* 16px */
}

.content {
    padding: var(--nes-spacing-lg); /* 24px */
}

/* ❌ НЕПРАВИЛЬНО - нечётные пиксели */
.layout {
    gap: 15px; /* Запрещено! */
}

.header {
    padding: 17px; /* Запрещено! */
}
```

### 🔍 Сравнение ошибок vs правил

```css
/* ❌ ОШИБКИ (нечётные пиксели) */
.element {
    padding: 5px;      /* ❌ Не кратно 4px */
    margin: 7px;       /* ❌ Не кратно 4px */
    gap: 13px;         /* ❌ Не кратно 4px */
    padding: 15px 22px; /* ❌ Оба значения не кратны 4px */
}

/* ✅ ПРАВИЛЬНО (кратно 4px) */
.element {
    padding: var(--nes-spacing-sm);      /* 8px ✅ */
    margin: var(--nes-spacing-md);       /* 16px ✅ */
    gap: var(--nes-spacing-md);         /* 16px ✅ */
    padding: var(--nes-spacing-md) var(--nes-spacing-lg); /* 16px 24px ✅ */
}
```

### 🎨 CSS-переменные для единообразия спейсинга

```css
:root {
    /* Базовые значения спейсинга (все кратны 4px) */
    --nes-spacing-xs: 4px;   /* Минимальный отступ */
    --nes-spacing-sm: 8px;   /* Малый отступ */
    --nes-spacing-md: 16px;  /* Средний отступ */
    --nes-spacing-lg: 24px;  /* Большой отступ */
    --nes-spacing-xl: 32px;  /* Очень большой отступ */
}
```

### 📋 Примеры использования

```css
/* ✅ ПРАВИЛЬНО - использование переменных */
.button {
    padding: var(--nes-spacing-sm) var(--nes-spacing-md); /* 8px 16px */
    margin-bottom: var(--nes-spacing-sm); /* 8px */
}

.card {
    padding: var(--nes-spacing-md); /* 16px */
    margin-bottom: var(--nes-spacing-lg); /* 24px */
}

.grid {
    gap: var(--nes-spacing-md); /* 16px */
}

.section {
    padding: var(--nes-spacing-lg); /* 24px */
    margin-bottom: var(--nes-spacing-xl); /* 32px */
}
```

### 🎯 Ключевой канон

**Все отступы кратны 4px, никогда нечётные пиксели!**

Это создаёт визуальную ритмичность и единообразие, характерное для классического 8-битного дизайна.

---

## 8. Пиксельное рендеринг изображений

### 🎯 Единое правило

**Все изображения должны использовать pixelated rendering для 8-битного стиля.**

### 📏 Стандарт

```css
image-rendering: pixelated;
image-rendering: -moz-crisp-edges;
image-rendering: crisp-edges;
```

### ✅ Где применяется

- Все изображения в 8-битной теме
- Все картинки в статьях
- Все иконки и графические элементы

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО - глобально для всех изображений */
body.nes8bit-theme img,
body.nes8bit-theme picture,
body.nes8bit-theme picture img {
    image-rendering: pixelated;
    image-rendering: -moz-crisp-edges;
    image-rendering: crisp-edges;
}
```

---

## 8. Цветовая палитра

### 🎯 Единое правило

**Используй только цвета из CSS-переменных, не хардкодь цвета.**

### 📏 Стандарт

```css
/* Основные цвета */
--nes-black: #000000;
--nes-white: #FFFFFF;
--nes-red: #E53B2C;
--nes-blue: #0099CC;
--nes-green: #228B22;
--nes-yellow: #FFD700;
--nes-pink: #FFB6C1;
--nes-gray: #808080;
```

### ✅ Где применяется

- Все фоны
- Все тексты
- Все рамки
- Все акценты

### ❌ Запрещено

- Хардкодить цвета (`color: #FF0000`)
- Использовать цвета вне палитры
- Только через CSS-переменные

### 📝 Примеры

```css
/* ✅ ПРАВИЛЬНО */
.button {
    background: var(--nes-yellow);
    color: var(--nes-black);
    border: var(--nes-border-width) solid var(--nes-black);
}

/* ❌ НЕПРАВИЛЬНО */
.button {
    background: #FFD700; /* Хардкод цвета! */
    color: #000; /* Хардкод цвета! */
}
```

---

## 10. Шрифты

### 🎯 Единое правило

**Используй только шрифт Press Start 2P для 8-битного стиля.**

### 📏 Стандарт

```css
--nes-font: 'Press Start 2P', 'Courier New', monospace;
```

### ✅ Где применяется

- Все тексты в 8-битной теме
- Все кнопки
- Все заголовки
- Все навигационные элементы

---

## 11. Чек-лист перед добавлением нового элемента

Перед добавлением нового элемента на сайт, проверь:

- [ ] Толщина линий = `var(--nes-border-width)` (2px)
- [ ] Скругление углов = `4px`
- [ ] БЕЗ теней (плоские элементы)
- [ ] Beveling применён (светлый верхний-левый, тёмный нижний-правый)
- [ ] Кнопки унифицированы (hover: жёлтый + translateY(-1px), active: translateY(1px) + инвертированный beveling)
- [ ] Анимации только вертикальные (`translateY(-1px)`)
- [ ] Отступы кратны 4px (используй `var(--nes-spacing-*)`)
- [ ] Изображения используют pixelated rendering
- [ ] Цвета из CSS-переменных
- [ ] Шрифт = `var(--nes-font)`

---

## 12. Исключения

**Исключений НЕТ!** Все правила применяются ко всем элементам без исключений.

---

## 13. Как использовать этот документ

1. **При добавлении нового элемента** — сверься с разделами 1-10
2. **При изменении существующего элемента** — проверь соответствие правилам
3. **При рефакторинге** — используй чек-лист из раздела 11
4. **При сомнениях** — следуй правилам, не делай исключений

---

## 📚 Ссылки на примеры

- [The Eight Arms 8-bit Games](https://onepagelove.com/the-eight-arms-8-bit-games) — пример единообразия
- [NES.css](https://dev.to/0xkoji/create-8bit-like-site-with-nes-css-2fjn) — CSS-фреймворк для 8-битного стиля
- [Rocket Fuel Restaurant](https://webflow.com/made-in-webflow/website/eatrocketfuel) — пример ретро-стиля

---

**Помни: Единообразие — ключ к качественному дизайну!**



