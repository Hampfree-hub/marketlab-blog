# 🎨 Переименование блога и изменение URL

## ✅ Можно ли переименовать?

**ДА!** Есть несколько вариантов:

---

## 🔄 Вариант 1: Переименовать репозиторий на GitHub

### Как это сделать:

1. **Откройте репозиторий:**
   - https://github.com/Hampfree-hub/hampfree-blog

2. **Settings → General → Repository name:**
   - Измените `hampfree-blog` на любое другое имя
   - Например: `my-blog`, `personal-blog`, `hampfree-site`

3. **После переименования:**
   - URL изменится автоматически
   - Новый URL: `https://hampfree-hub.github.io/НОВОЕ_ИМЯ`

4. **Обновить локально:**
   ```powershell
   # Обновить remote URL
   git remote set-url github https://github.com/Hampfree-hub/НОВОЕ_ИМЯ.git
   
   # Обновить astro.config.mjs
   # Изменить base: '/НОВОЕ_ИМЯ'
   ```

---

## 🌐 Вариант 2: Кастомный домен (самый красивый!)

### Можно использовать свой домен:

**Примеры:**
- `hampfree.com`
- `blog.hampfree.com`
- `hampfree.dev`

### Как настроить:

1. **Купить домен** (например, на reg.ru, ~200₽/год)

2. **В GitHub:**
   - Settings → Pages → Custom domain
   - Введите ваш домен: `hampfree.com`

3. **Настроить DNS:**
   - Тип: `CNAME`
   - Имя: `@` или `www`
   - Значение: `hampfree-hub.github.io`

4. **Обновить astro.config.mjs:**
   ```javascript
   site: 'https://hampfree.com',  // Ваш домен
   base: '/',  // Убрать base, если домен корневой
   ```

**Результат:** Красивый URL вместо `hampfree-hub.github.io/hampfree-blog`!

---

## 📝 Вариант 3: Оставить как есть (тренировка)

**Можно:**
- Потренироваться с текущим названием
- Понять как всё работает
- Потом переименовать когда будете готовы

**Переименование займёт 2 минуты!**

---

## 🎯 Рекомендации

### Для тренировки:
- ✅ Оставить `hampfree-blog` — нормальное название
- ✅ Потренироваться, понять процесс
- ✅ Потом переименовать когда будете готовы

### Для продакшена:
- ✅ Использовать кастомный домен (самый красивый вариант)
- ✅ Или переименовать репозиторий в более короткое имя

---

## 🔧 Что нужно обновить при переименовании

1. **GitHub репозиторий** (Settings → Repository name)
2. **Remote URL** (`git remote set-url`)
3. **astro.config.mjs** (поле `base`)
4. **GitHub Pages** (автоматически обновится)

**Всё это займёт 5 минут!**

---

## 💡 Идеи для названий

- `blog` — коротко и ясно
- `site` — универсально
- `hampfree` — ваше имя
- `hampfree-dev` — для разработки
- `personal-blog` — личный блог

**Или использовать кастомный домен — самый красивый вариант!** 🎨

