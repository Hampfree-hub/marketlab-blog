# 📁 Скрипты проекта

**⚠️ КРИТИЧЕСКИ ВАЖНО: Все задачи, тикеты, доска проектов, теги, описания, колонки — только через GitHub!**

---

## 🔄 auto-push-to-private.ps1

**Назначение:** Автоматическая отправка заблокированных файлов (`*_PLAN.md` и т.д.) в приватный репозиторий.

**Использование:**
- Вызывается автоматически из pre-commit hook
- Или вручную: `.\scripts\auto-push-to-private.ps1 -BlockedFiles "file1.md", "file2.md"`

**Путь к приватному репозиторию:**
- По умолчанию: `../HampfreeBlog-Private`
- Можно изменить в скрипте

---

## 🔒 pre-commit

**Назначение:** Pre-commit hook для проверки на секреты и запрещённые файлы.

**Установка:**
```powershell
Copy-Item scripts\pre-commit .git\hooks\pre-commit -Force
```

**Функции:**
- Проверка на секреты (токены, пароли)
- Проверка на запрещённые файлы (`*_PLAN.md`, `*_STRATEGY.md`)
- Автоматическая отправка в приватный репозиторий
- Проверка на `.env` файлы

---

## ✅ ГАРАНТИИ

**Скрипты сохранены в репозитории:**
- ✅ `auto-push-to-private.ps1` — в репозитории
- ✅ `pre-commit` — в репозитории (копия hook'а)

**После перезагрузки:**
- ✅ Скрипты останутся (они в Git)
- ✅ Нужно только установить hook: `Copy-Item scripts\pre-commit .git\hooks\pre-commit`

---

**Всё готово!** ✅



