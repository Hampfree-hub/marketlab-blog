# 🔧 Правила обработки ошибок VPN/сети

**Дата:** 20.12.2025

---

## ⚠️ ПРОБЛЕМА

При работе с GitHub API, Git операциями и другими внешними сервисами возникают ошибки:
- "Connection failed"
- "Request ID: xxx-xxx-xxx"
- "VPN error"
- "Internet connection error"

**Текущее поведение:** AI просит пользователя проверить VPN/интернет → теряется время

**Цель:** Автоматически обрабатывать ошибки, не отвлекая пользователя

---

## ✅ ПРАВИЛА ОБРАБОТКИ ОШИБОК

### 1. GitHub API ошибки

**Симптомы:**
- `Invoke-RestMethod: Connection failed`
- `Request ID: xxx-xxx-xxx`
- HTTP статусы: 500, 502, 503, 504
- Таймауты

**Действия:**
1. ✅ **НЕ просить пользователя проверить VPN**
2. ✅ **Повторить запрос 2-3 раза** с задержкой 2-5 секунд
3. ✅ **Если не помогло** — использовать альтернативный метод:
   - Вместо API — использовать `git` команды
   - Вместо GraphQL — использовать REST API
   - Вместо прямого запроса — использовать кэш/локальные данные
4. ✅ **Если всё равно не работает** — сообщить пользователю кратко: "Временная проблема с GitHub API, продолжаю через Git команды"

**Пример:**
```powershell
# ❌ НЕПРАВИЛЬНО:
Write-Host "Проверьте VPN и интернет соединение" -ForegroundColor Red

# ✅ ПРАВИЛЬНО:
$retryCount = 0
$maxRetries = 3
while ($retryCount -lt $maxRetries) {
    try {
        $response = Invoke-RestMethod -Uri $url -Headers $headers
        break
    } catch {
        $retryCount++
        if ($retryCount -lt $maxRetries) {
            Start-Sleep -Seconds 3
            continue
        } else {
            # Использовать альтернативный метод
            Write-Host "GitHub API временно недоступен, использую Git команды" -ForegroundColor Yellow
            # Продолжить через git
        }
    }
}
```

---

### 2. Git операции ошибки

**Симптомы:**
- `fatal: unable to access 'https://github.com/...': Failed to connect`
- `Connection timed out`
- `SSL/TLS connection failed`

**Действия:**
1. ✅ **НЕ просить пользователя проверить VPN**
2. ✅ **Проверить локальное состояние** (git status, git log)
3. ✅ **Использовать локальные операции** (git add, git commit)
4. ✅ **Отложить push** — сообщить: "Изменения закоммичены локально, push выполним позже"

**Пример:**
```powershell
# ❌ НЕПРАВИЛЬНО:
Write-Host "Проверьте VPN перед push" -ForegroundColor Red

# ✅ ПРАВИЛЬНО:
try {
    git push origin branch-name
} catch {
    Write-Host "Push отложен (временная проблема с сетью), изменения сохранены локально" -ForegroundColor Yellow
    # Продолжить работу
}
```

---

### 3. Общие сетевые ошибки

**Симптомы:**
- `Connection failed`
- `Request ID: xxx`
- Любые ошибки с "connection", "timeout", "network"

**Действия:**
1. ✅ **Автоматический retry** (2-3 попытки)
2. ✅ **Использовать альтернативные методы** (локальные операции, кэш)
3. ✅ **Продолжить работу** без остановки
4. ✅ **Краткое сообщение** пользователю (без просьбы проверить VPN)

**Шаблон сообщения:**
```
⚠️ Временная проблема с [сервис]. Продолжаю через [альтернативный метод].
Изменения сохранены локально.
```

---

## 📋 ЧЕКЛИСТ ПРИ ОШИБКАХ

**При любой сетевой ошибке:**

- [ ] Автоматический retry (2-3 попытки)
- [ ] Использовать альтернативный метод
- [ ] Продолжить работу без остановки
- [ ] НЕ просить пользователя проверить VPN
- [ ] Краткое информативное сообщение

---

## 🎯 ПРИМЕРЫ ПРАВИЛЬНОГО ПОВЕДЕНИЯ

### Пример 1: GitHub API ошибка
```
❌ НЕПРАВИЛЬНО:
"Проверьте VPN и интернет соединение. Request ID: xxx"

✅ ПРАВИЛЬНО:
"⚠️ Временная проблема с GitHub API. Использую Git команды для проверки статуса."
[Продолжает работу через git status, git log]
```

### Пример 2: Git push ошибка
```
❌ НЕПРАВИЛЬНО:
"Ошибка push. Проверьте VPN."

✅ ПРАВИЛЬНО:
"⚠️ Push отложен (временная проблема с сетью). Изменения сохранены локально."
[Продолжает работу, предлагает push позже]
```

### Пример 3: Множественные ошибки
```
❌ НЕПРАВИЛЬНО:
"У вас проблемы с VPN. Проверьте соединение."

✅ ПРАВИЛЬНО:
"⚠️ Временные проблемы с внешними сервисами. Работаю в офлайн-режиме:
- Изменения сохранены локально
- Git операции выполнены
- Push можно выполнить позже
Продолжаю работу над задачей."
```

---

## 🔄 АВТОМАТИЧЕСКИЙ RETRY

**Для всех сетевых операций:**

```powershell
function Invoke-WithRetry {
    param(
        [scriptblock]$ScriptBlock,
        [int]$MaxRetries = 3,
        [int]$DelaySeconds = 2
    )
    
    $retryCount = 0
    while ($retryCount -lt $MaxRetries) {
        try {
            return & $ScriptBlock
        } catch {
            $retryCount++
            if ($retryCount -lt $MaxRetries) {
                Start-Sleep -Seconds $DelaySeconds
                continue
            } else {
                throw "Max retries exceeded: $_"
            }
        }
    }
}

# Использование:
$response = Invoke-WithRetry -ScriptBlock {
    Invoke-RestMethod -Uri $url -Headers $headers
}
```

---

## 📝 ПРАВИЛА ДЛЯ AI

**КРИТИЧЕСКИ ВАЖНО:**

1. ❌ **НИКОГДА не просить пользователя проверить VPN/интернет**
2. ✅ **ВСЕГДА использовать retry механизм**
3. ✅ **ВСЕГДА иметь альтернативный метод**
4. ✅ **ВСЕГДА продолжать работу** (не останавливаться на ошибке)
5. ✅ **Краткие информативные сообщения** (без паники)

**Если ошибка критична:**
- Сохранить состояние работы
- Предложить альтернативный путь
- НЕ просить пользователя что-то проверить

---

## ✅ ИТОГ

**Цель:** Минимизировать потери времени на проверку VPN/интернета

**Метод:** Автоматическая обработка ошибок + альтернативные методы

**Результат:** Работа продолжается без остановок, пользователь не отвлекается

---

**Этот документ должен быть в памяти AI при работе с сетевыми операциями!**
