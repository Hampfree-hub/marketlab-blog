# 📦 Перемещение файлов из подпапки в корень

## 🔧 Проблема

Astro создал проект в подпапке `violet-venus` потому что директория не пустая.

## ✅ Решение

### Выполните в терминале:

```powershell
cd "D:\Projects\HampfreeBlog"

# Переместить всё из violet-venus в корень (кроме node_modules - переустановим)
Get-ChildItem -Path "violet-venus" -Exclude "node_modules" | Move-Item -Destination . -Force

# Удалить пустые папки
Remove-Item -Recurse -Force "afraid-apogee", "terrestrial-transit", "violet-venus" -ErrorAction SilentlyContinue

# Установить зависимости
npm install

# Обновить astro.config.mjs (заменить base на /marketlab-blog)
# Файл уже правильный, но проверим
```

---

## 📝 После перемещения

1. Проверить что `package.json` есть в корне
2. Проверить что `src/` есть в корне
3. Обновить `astro.config.mjs` (уже готово!)
4. Собрать проект: `npm run build`

---

**Выполните команды выше!** 🚀

