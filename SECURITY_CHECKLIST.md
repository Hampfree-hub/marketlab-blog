# ✅ Чеклист безопасности — MarketLab Blog

**Дата:** 12.12.2025  
**Статус:** Готов к проверке

---

## 📋 Что уже защищено автоматически:

### ✅ HTTPS (GitHub Pages)
- [x] **HTTPS работает автоматически** — GitHub Pages предоставляет SSL-сертификат
- [x] **Принудительное перенаправление** — HTTP → HTTPS автоматически
- [ ] **Проверить вручную:** Открыть `https://hampfree-hub.github.io/marketlab-blog/`
  - Должен быть 🔒 в адресной строке
  - Не должно быть ⚠️ "Небезопасно"

### ✅ DDoS защита (GitHub Pages)
- [x] **CDN защита** — контент раздаётся с серверов по всему миру
- [x] **Автоматическая защита** — GitHub обрабатывает миллионы запросов
- [x] **Rate limiting** — ограничение запросов от одного IP
- [x] **Мощная инфраструктура** — выдерживает большие нагрузки

### ✅ XSS защита (Astro)
- [x] **Автоматическое экранирование** — Astro превращает опасные символы в безопасные
- [x] **Защита по умолчанию** — работает без дополнительной настройки
- [ ] **Проверить вручную:** Если будут формы (комментарии, поиск) → проверить что они безопасны

---

## ⚠️ Что нужно проверить/настроить:

### 1. Проверить XSS защита

**Что проверить:**
- [ ] Если будут формы (комментарии, поиск) → убедиться что Astro их обрабатывает безопасно
- [ ] Если будет пользовательский контент → убедиться что он экранируется
- [ ] Проверить что `<script>` теги не выполняются (превращаются в текст)

**Как проверить:**
- [ ] Создать тестовую форму (если будет)
- [ ] Ввести `<script>alert('test')</script>` в поле
- [ ] Проверить что код не выполняется (не появляется alert)

**Статус:** ⏳ Нужно проверить когда будут формы

---

### 2. Настроить Security Headers

#### 2.1. CSP (Content Security Policy)

**Что это:** Список разрешённых источников для скриптов, стилей, изображений

**Что сделано:**
- [x] Создан middleware с базовой CSP политикой
- [x] Настроен CSP header в `src/middleware.ts`
- [ ] Определить какие внешние ресурсы нужны (Google Analytics, шрифты, и т.д.) — для кастомизации позже
- [ ] Протестировать что всё работает в production (нужен адаптер)

**Пример:**
```javascript
// Разрешены скрипты только с нашего сайта и Google Analytics
Content-Security-Policy: script-src 'self' 'unsafe-inline' 'unsafe-eval'
```

**Статус:** ✅ Настроено (работает в dev, для production нужен адаптер)

---

#### 2.2. HSTS (HTTP Strict Transport Security)

**Что это:** Принудительное использование HTTPS

**Что сделано:**
- [x] Настроен HSTS header в `src/middleware.ts`
- [x] Установлен срок действия: 1 год (max-age=31536000)
- [x] Добавлены includeSubDomains и preload
- [ ] Проверить что работает в production (нужен адаптер)

**Пример:**
```javascript
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
```

**Примечание:** GitHub Pages уже делает это автоматически, но мы усилили

**Статус:** ✅ Настроено (работает в dev, для production нужен адаптер)

---

#### 2.3. X-Frame-Options

**Что это:** Защита от встраивания сайта в iframe (защита от clickjacking)

**Что сделано:**
- [x] Настроен X-Frame-Options header в `src/middleware.ts`
- [x] Установлено значение: `DENY` (запретить встраивание)
- [ ] Проверить что работает в production (нужен адаптер)

**Пример:**
```javascript
X-Frame-Options: DENY
```

**Статус:** ✅ Настроено (работает в dev, для production нужен адаптер)

---

#### 2.4. X-Content-Type-Options

**Что это:** Браузер не должен "угадывать" тип файла (защита от MIME-sniffing)

**Что сделано:**
- [x] Настроен X-Content-Type-Options header в `src/middleware.ts`
- [x] Установлено значение: `nosniff`
- [ ] Проверить что работает в production (нужен адаптер)

**Пример:**
```javascript
X-Content-Type-Options: nosniff
```

**Статус:** ✅ Настроено (работает в dev, для production нужен адаптер)

---

#### 2.5. Referrer-Policy

**Что это:** Контроль какой информации отправляется в Referrer

**Что сделано:**
- [x] Выбрана политика: `strict-origin-when-cross-origin`
- [x] Настроен Referrer-Policy header в `src/middleware.ts`
- [ ] Проверить что работает в production (нужен адаптер)

**Пример:**
```javascript
Referrer-Policy: strict-origin-when-cross-origin
```

**Статус:** ✅ Настроено (работает в dev, для production нужен адаптер)

---

### 3. Проверить HTTPS работает

**Что проверить:**
- [ ] Открыть `https://hampfree-hub.github.io/marketlab-blog/`
- [ ] Проверить что есть 🔒 в адресной строке
- [ ] Проверить что нет ⚠️ "Небезопасно"
- [ ] Попробовать открыть `http://...` (без s) → должно перенаправить на HTTPS

**Статус:** ⏳ Нужно проверить

---

### 4. Проверить нет "смешанного контента"

**Что это:** HTTP ресурсы на HTTPS странице (небезопасно!)

**Что проверить:**
- [ ] Открыть сайт в браузере
- [ ] Открыть DevTools (F12) → Console
- [ ] Проверить нет ошибок типа "Mixed Content"
- [ ] Проверить что все изображения, скрипты, стили загружаются через HTTPS

**Пример проблемы:**
```html
<!-- ❌ НЕПРАВИЛЬНО (HTTP на HTTPS странице): -->
<img src="http://example.com/image.jpg">

<!-- ✅ ПРАВИЛЬНО (HTTPS): -->
<img src="https://example.com/image.jpg">
```

**Статус:** ⏳ Нужно проверить

---

## 📊 Приоритеты:

### 🔴 Критически важно (сделать в первую очередь):
1. ✅ HTTPS — уже работает автоматически
2. ✅ DDoS защита — уже работает автоматически
3. ✅ XSS защита — уже работает автоматически
4. ⏳ Проверить HTTPS работает (вручную)
5. ⏳ Проверить нет "смешанного контента"

### 🟡 Важно (сделать потом):
1. ⏳ Настроить Security Headers (CSP, HSTS, X-Frame-Options, и т.д.)
2. ⏳ Проверить XSS защита (когда будут формы)

### 🟢 Опционально (можно позже):
1. ⏳ Усилить HSTS (GitHub Pages уже делает, но можно улучшить)

---

## 🚀 План действий:

### Шаг 1: Проверить что уже работает (5 минут)
- [ ] Открыть сайт в браузере
- [ ] Проверить HTTPS работает (🔒 в адресной строке)
- [ ] Проверить нет ошибок в Console

### Шаг 2: Настроить Security Headers (30-60 минут)
- [ ] Создать файл для headers в Astro
- [ ] Настроить CSP, HSTS, X-Frame-Options, и т.д.
- [ ] Протестировать что всё работает

### Шаг 3: Проверить XSS защита (когда будут формы)
- [ ] Создать тестовую форму
- [ ] Проверить что экранирование работает

---

## 📚 Ресурсы:

- **SECURITY_EXPLAINED.md** — подробное объяснение безопасности
- **SECURITY_SIMPLE_EXPLAINED.md** — очень простое объяснение для новичков
- [Astro Security Documentation](https://docs.astro.build/en/guides/security/)
- [GitHub Pages Security](https://docs.github.com/en/pages/getting-started-with-github-pages/about-github-pages)

---

**Готовы начать проверку?** 🚀

