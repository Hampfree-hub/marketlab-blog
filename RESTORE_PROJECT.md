# 🔧 Восстановление структуры проекта

## ❌ Проблема

Папка `src` и другие файлы Astro отсутствуют в рабочей директории.

## ✅ Решение

### Вариант 1: Взять из GitHub (если там есть)

Файлы могли быть запушены в GitHub, но не в GitLab. Проверьте:

1. Откройте: https://github.com/Hampfree-hub/marketlab-blog
2. Проверьте есть ли папка `src/` в репозитории
3. Если есть — выполните:

```powershell
cd "D:\Projects\HampfreeBlog"
git fetch github
git checkout github/main -- src public tsconfig.json
```

### Вариант 2: Создать структуру заново

Если файлов нет нигде, нужно создать базовую структуру Astro проекта.

---

## 📝 Напоминание: Два репозитория

### 1. GitLab (origin) — основной
- **URL:** `https://gitlab.com/hampfree-team-group/hampfree-blog`
- **Назначение:** Основная разработка
- **Remote:** `origin`
- **Команда:** `git push origin main`

### 2. GitHub (github) — для хостинга
- **URL:** `https://github.com/Hampfree-hub/marketlab-blog`
- **Назначение:** GitHub Pages (хостинг блога)
- **Remote:** `github`
- **Команда:** `git push github main`

**Зачем два:**
- ✅ Резервирование (если один недоступен)
- ✅ GitHub Pages работает только с GitHub
- ✅ GitLab для разработки, GitHub для хостинга

**Рабочий процесс:**
```powershell
# Коммитить изменения
git add .
git commit -m "описание"

# Пушить в оба репозитория
git push origin main   # GitLab (основной)
git push github main   # GitHub (автодеплой)
```

---

**Сначала проверьте что есть в GitHub репозитории!**

